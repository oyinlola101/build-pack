#!/usr/bin/env bash

# Check for a file (e.g., package.json) that indicates this buildpack should be used
if [ -f .dockerignore ]; then
  echo "Environment config By Oyin"
  exit 0
else
  exit 1
fi


